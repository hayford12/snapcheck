{
  "name": "snapcheck",
  "version": "1.0.0",
  "scripts": {
    "build": "cd snapcheck-client && npm install && npm run build && cp -r dist ../snapcheck-server/public",
    "start": "cd snapcheck-server && node src/index.js",
    "install": "cd snapcheck-server && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
